{
  "name": "devtask-ledger-frontend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "dev:docker": "docker run --rm -it -v $(pwd):/app -w /app -p 5173:5173 node:22-bullseye sh -c \"npm ci --no-audit --no-fund && npm run dev -- --host 0.0.0.0\"",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier --write src/**/*.ts"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.1",
    "@stacks/connect": "^7.8.2",
    "@stacks/network": "^7.3.1",
    "@stacks/transactions": "^7.3.1",
    "chart.js": "^4.5.1",
    "sats-connect": "^3.5.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  }
}
